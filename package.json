{
  "name": "secure-chat",
  "version": "1.0.0",
  "description": "Secure P2P chat",
  "main": "app.js",
  "scripts": {
    "all": "npm install && npm run setup && npm run build && npm run start",
    "bnr": "npm run build && npm run start",
    "clean": "rm -rf certs/ app/",
    "setup": "mkdir certs && openssl genrsa -out ./certs/rsa.pem 2048 && openssl rsa -in ./certs/rsa.pem -pubout -out certs/rsa.pub",
    "build": "./node_modules/.bin/babel src -d app",
    "start": "node ./app/main.js",
    "start-temp": "node ./app/main.js --temp"
  },
  "author": "Joseph Dalrymple <me@swivel.in>",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-core": "^6.9.1",
    "babel-preset-es2015": "^6.9.0"
  },
  "dependencies": {
    "ursa": "^0.9.4",
    "ws": "^1.1.0",
    "yargs": "^4.7.1"
  }
}
